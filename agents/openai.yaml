interface:
  display_name: "Ticket Train"
  short_description: "Run verified, cost-controlled ticket trains"
  default_prompt: "Use $ticket-train with an explicit source and selection to run a visible train with parallel independent acceptance tests, red/green environment gates, strict routing, bounded reviews, durable recovery, and token accounting."

policy:
  allow_implicit_invocation: false
