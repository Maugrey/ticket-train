interface:
  display_name: "Ticket Train"
  short_description: "Run verified, cost-controlled ticket trains"
  default_prompt: "Use $ticket-train with an explicit source and selection to run a visible train with a deterministic native runner, independent acceptance tests and reviews, durable recovery, explicit scope decisions, and measured token usage."

policy:
  allow_implicit_invocation: false
